import React from "react"

const UserContext = React.createContext()


export default UserContext;












// the component direclty can take the access of all state  
{/* <UserContext>
    <Login/>
    <Card>
        <Data/>
    </Card>

</UserContext> */}
